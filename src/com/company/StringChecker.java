package com.company;

public interface StringChecker {
    boolean isValid(String checkFor)
    {
        if (str.indexOf(checkFor)) = 0) || (str.length() > min) || (str.length < max)
            {
                return false;
            }
        else
        {
            return true;
        }
    }
}
